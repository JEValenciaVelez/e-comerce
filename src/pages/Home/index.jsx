import { Layout } from "../../components/Layout"
import { NavBar } from "../../components/NavBar"


export const Home = () => {

    return (
        <Layout>
            <NavBar />
        </Layout>
    )
}